#include "tokenizer.h"

using namespace slurp;

Tokenizer::Tokenizer( const std::string& data ) {
    this->data = data;
}

std::string Tokenizer::next() {
    std::string next;

    /* stub */

    return next;
}
