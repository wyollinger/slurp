add a pthread for each get/parse/queue
finish networking code
port entire project to C, switching from awful generated flex scanner classes to reentrant C routines
fix the flex ruleset, it is badly broken

