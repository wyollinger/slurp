write debug messages to a file as well as the console.

migrate to a totally signal based system, QWebPages must all be created and accessed on the gui thread.

periodically write results of crawl to the disk
