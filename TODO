rewrite the debugging info handling system to include timing and thread information, and allow for filtering at run-time
write it to a file as well as the console 

link in libdb

test windows builds

add GUI support
